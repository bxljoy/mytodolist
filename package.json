{
  "name": "mytodolist",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "if-env TYPE=sever && ( cd server && npm install ) || ( cd client && npm install )",
    "server": "node ./server/server.js",
    "client": "cd client && npm start"
  },
  "dependencies": {
    "if-env": "^1.0.4"
  }
}
